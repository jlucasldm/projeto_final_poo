public enum EnumGenero {
    POP_ENUM,
    RAP_ENUM,
    HIPHOP_ENUM,
    LOUVOR_ENUM,
    INDIE_ENUM,
    ARTPOP_ENUM,
    ROCK_ENUM,
    SERTANEJO_ENUM,
    PAGODE_ENUM,
    PISEIRO_ENUM,
    MPB_ENUM,
    EDM_ENUM,
    RANDB_ENUM,
    CLASSICA_ENUM,
    SOUL_ENUM,
    BLUES_ENUM,
    PUNK_ENUM,
    KPOP_ENUM,
    SAMBA_ENUM,
    VAQUEJADA_ENUM,
    ARROCHA_ENUM,
    PAGODAO_ENUM;
}
